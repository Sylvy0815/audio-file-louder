# audio-file-louder

오디오 파일(mp3 등)의 db을 높일 수 있는 프로젝트입니다.

## INSTALL

```sh
brew install ffmpeg (시간 좀 걸립니다..)
python3 -m venv audio-file-louder-venv
```

## START

```sh
source audio-file-louder-venv/bin/activate
pip3 install -r requirements.txt
python3 main.py
```
